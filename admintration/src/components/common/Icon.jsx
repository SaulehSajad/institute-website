import React from "react";

const Icon = ({ children }) => (
  <span className="inline-flex items-center justify-center w-8 h-8 rounded-md bg-white/60 shadow-sm text-black/70">
    {children}
  </span>
);

export default Icon;
